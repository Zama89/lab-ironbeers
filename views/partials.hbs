<div>
    {{#if this.image_url}}
    <img src='{{this.image_url}}' class='img-beer' alt='{{this.name}}' />
    {{else}}
    <p> Not image </p>
    {{/if}}
    <div>
        <h2>{{this.name}}</h2>
        <p>{{this.tagline}}</p>
        <p>{{this.description}}</p>
    </div>
</div>